// Single source of truth for hideable features.
// Flip to true to re-enable My Collection.
export const COLLECTION_FEATURE_ENABLED = false;

// Flip to true to re-enable Research Library (DB-backed knowledge_articles surface).
export const RESEARCH_LIBRARY_FEATURE_ENABLED = false;
